<!--
개정: 2026-09-10 (원본: src/content/reviews/rlhf.md 를 4편으로 분리한 것 중 3편)
- 원본의 「Training language models to follow instructions with human feedback」 절을 독립시켰다
- 그림은 /img/rlhf/04.png 를 그대로 참조한다
- 원본의 "PPO-ptx 도입: 사전 훈련 분포의 로그 가능도를 높이는 업데이트를 혼합하여"
  라는 문장이 "혼합하여" 에서 끊겨 있었다. 무엇을 막기 위한 것인지(alignment tax,
  공개 NLP 벤치마크 성능 저하) 를 채워 완성했다
- 원본의 문체가 "~합니다" 체와 "~함" 체가 섞여 있어 평서형으로 통일
- 「3단계 절차」를 SFT / 보상 모델 / PPO 로 명시적으로 구분. 원본은 번호가 이어져 있었다
- 원본의 「한계」와 「함의」 서술은 그대로 유지. 특히 "누구에게 정렬되었는가" 지적과
  "큰 모델을 새로 훈련하는 것보다 기존 모델을 정렬하는 게 효율적일 수 있다"는 관찰이
  이 글의 값어치다
-->
---
title: InstructGPT
paper: Training language models to follow instructions with human feedback
venue: NeurIPS 2022
authors: Long Ouyang, Jeff Wu, Xu Jiang, et al.
link: https://arxiv.org/abs/2203.02155
claim: 모델을 키우는 것만으로는 사용자 의도를 따르지 않는다. 사람 선호로 정렬하면 100배 작은 모델이 더 선호된다.
tags: [RLHF, LLM, Alignment]
tier: basic
date: 2025-04-16
draft: true
---

핵심 키워드:
alignment, SFT → 보상 모델 → PPO 3단계, PPO-ptx, Helpful·Honest·Harmless
주요 전략:
사람이 쓴 예시 답변으로 먼저 지도학습하고, 사람의 순위 데이터로 보상 모델을 만들어 PPO로 최적화한다
사용 가능 분야:
지시 따르기, 그리고 이후 대부분의 대화형 LLM 학습 절차

#### 문제 인식

**대규모 언어 모델은 크기를 키운다고 사용자의 의도를 더 잘 따르지 않는다.**

실제로는 다음과 같은 의도하지 않은 행동을 자주 보인다.

- 허위 정보 생성
- 유해한 내용 생성
- 편향된 출력
- 단순히 사용자 지시를 따르지 않음

**정렬(alignment)의 목적**

언어 모델이 사용자의 **명시적 의도**(지시 따르기)뿐만 아니라, 진실성·비편향성·무해성 같은 **암묵적 의도**에 따라 행동하도록 훈련하는 것이다. 논문은 모델이 **유용하고(Helpful), 정직하고(Honest), 무해해야(Harmless)** 한다고 명시한다.

#### 3단계 절차

![그림 4](/img/rlhf/04.png)

**1단계 — 지도 파인튜닝 (SFT)**

1. prompt dataset을 가져온다.
2. 라벨러가 각 prompt에 대한 **좋은 예시 답변**을 직접 작성한다.
3. 이것으로 GPT-3를 지도학습 파인튜닝해서 예시 답변과 비슷한 답변이 나오게 한다.

여기서 학습되는 것은 **질문에서 답변으로의 매핑**이다. 좋은 답변 텍스트의 전체 토큰을 사용해 모델의 답변과 토큰 단위로 예측을 비교하고, 토큰 단위 loss로 학습한다.

**2단계 — 보상 모델 학습**

1. 프롬프트와 그에 대한 여러 모델의 출력을 가져온다.

   > *"most of our comparison data comes from our supervised policies, with some coming from our PPO policies"*

   대부분은 1단계 모델의 출력을 쓰고, 일부는 PPO로 정책을 갱신한 모델의 출력을 가져온다.

2. 이것을 사람의 판단으로 **순위를 매겨 비교쌍**으로 만든다. 인간의 선호를 학습할 수 있는 데이터가 된다.

   예를 들어 `B > A > C > D`라면 `B>A`, `B>C`, `B>D`, `A>C`, `A>D` 같은 쌍으로 분해한다. 선호되는 응답이 더 높은 점수를 받는 구조다.

3. 이 데이터로 보상 모델을 학습한다.

```python
loss = -E[log(σ(r_θ(x, y_w) - r_θ(x, y_l)))]

# r_θ(x, y_w): 선호된 응답의 보상
# r_θ(x, y_l): 덜 선호된 응답의 보상
# σ: sigmoid 함수
```

**3단계 — PPO로 정책 최적화**

새 프롬프트를 가져와서 보상 모델의 점수를 보상으로 삼아 정책을 갱신한다.

#### PPO-ptx

**문제** — 보상 모델만 최적화하면 **공개 NLP 벤치마크 성능이 떨어진다.** 정렬을 얻는 대가로 기존 능력을 잃는 것이고, 논문은 이를 **alignment tax**라고 부른다.

**해결** — PPO에 **사전 훈련 분포의 로그 가능도를 높이는 업데이트를 혼합**한다. 정렬 목표를 좇으면서도 원래 언어 모델링 능력을 붙잡아두는 것이다.

이 형태는 익숙하다. **"향상 도중 잃는 축을 방어한다"**는 점에서 WiSE-FT나 KL 정박과 목적이 같다. 구현하는 공간만 다르다.

#### 결과

- **GPT-3보다 파라미터가 100배 적은데도** 인간 선호가 더 높다.
- 할루시네이션이 **절반으로** 줄었다.
- respectful prompt가 주어졌을 때 GPT-3보다 **유해한 출력이 25% 감소**했다.

#### 한계

**누구에게 정렬되었는가**의 문제가 남는다. OpenAI API 고객의 선호에 정렬된 것이므로, 넓은 범위의 '사람'의 선호가 아니라 **특정 계층의 선호**를 따른다.

그리고 유해성과 무관한 생성이라 **유해한 정보를 생성할 수 있다.** 심지어 모델이 특정 사용자 지시를 따르지 않을 수도 있다.

#### 함의

**정렬 연구가 왜 필요한가.**

model pretraining에 비해 정렬을 위한 훈련 비용이 상대적으로 적다. 그렇다면 **큰 모델을 새로 훈련하는 것보다 기존 모델을 정렬하는 데 힘쓰는 게 더 효율적일 수 있다.**

이 관찰이 이후 방향을 상당 부분 결정했다. 100배 작은 모델이 정렬만으로 더 선호된다는 결과가 그 근거다.

#### 정리

InstructGPT가 확립한 것은 **SFT → 보상 모델 → PPO**라는 3단계 절차다. 이후 거의 모든 대화형 LLM이 이 골격을 따른다.

그리고 함께 드러난 것이 **alignment tax**다. 한 방향으로 최적화하면 다른 축을 잃는다는 것이고, 그래서 PPO-ptx처럼 원래 분포를 붙잡는 항이 필요해진다. 이 긴장은 이 계열 전체에서 반복된다.
